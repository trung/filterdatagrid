Current the grid is not working, I'm redesigning the interfaces so that it can have customized header renderer.

Also having some issues with performance when there are huge number of items in the datagrid
